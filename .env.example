# FastAPI Server Configuration
HOST=0.0.0.0
PORT=8000

# Model Configuration
# You can use "Qwen/Qwen2.5-1.5B-Instruct" or a local absolute path to the model directory
MODEL_ID=Qwen/Qwen2.5-1.5B-Instruct
DEVICE=auto
MAX_NEW_TOKENS=2048

# Security Settings (Optional)
# If set, requests must include Header: "Authorization: Bearer <API_KEY>"
API_KEY=
